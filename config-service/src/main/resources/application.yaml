server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: gateway
          uri: https://github.com/GustafffSoft/gateway/tree/servicios
          search-paths: config-data
  application:
    name: config-service
